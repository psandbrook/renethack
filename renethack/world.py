class GameWorld:

    def __init__(self, levels, level_length, character):
        """
        Create a procedurally generated game world with `level` number
        of levels, with each level having a side length of `level_length`.
        """
